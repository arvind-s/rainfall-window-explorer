# Python
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# Large source caches (NOT needed at runtime; regenerated by build_dataset.py)
data/raw_cache/
data/imd_cache/
data/imd_rt_cache/
data/out_test*/
data/*.log

# NOTE: data/out/ IS committed on purpose -- the app reads these baked
# parquet/geojson files at runtime (Streamlit Cloud cannot run the download).

# stray duplicate folder (not part of the project)
data copy/

# generated static HTML export (run generate_html.py to produce)
html_out/
